const fs = require('fs'); let b64 = fs.readFileSync('client/new_logo.png').toString('base64'); let html = fs.readFileSync('client/index.html', 'utf8'); html = html.replace(/src="data:image\/jpeg;base64,[^"]+"/g, 'src="data:image/png;base64,' + b64 + '"'); html = html.replace(/ style={{ clipPath: \'inset\(3px\)\' }}/g, ''); fs.writeFileSync('client/index.html', html); console.log('Reverted and embedded new logo exactly');
